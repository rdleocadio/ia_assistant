<div class="row">
  <div class="col">
    <h1><%= @tarefa.name %></h1>
    <%= raw(render_markdown(@challenge.content)) %>
  </div>
  <div class="col-md-3">
    <h2>Chats</h2>
    <div class="mb-4">
      <% if @chats.any? %>
        <ul>
          <% @chats.each do |chat| %>
            <li><%= link_to chat.title, chat_path(chat) %></li>
          <% end %>
        </ul>
      <% else %>
        There are no chats yet.
      <% end %>
    </div>
  </div>
</div>

<div class="row">
  <div class="col">
    <h1><%= @tarefa.name %></h1>
    <%= raw(render_markdown(@tarefa.content)) %>
  </div>
  <div class="col-md-3">
    <h2>Chats</h2>
    <!-- ... -->

    <%= button_to "New chat", challenge_chats_path(@tarefa), class: "btn btn-primary" %>
  </div>
</div>
